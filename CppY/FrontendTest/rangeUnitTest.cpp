#include "pch.h"
//#include "../Base/Types/range.h"
//using namespace py;

TEST(range, startAttributeIsReadableYetUnchangable)
{
	// Arrange
	//range r(4, 10, 2);
	//
	//// Act
	//int startVal = r.attr(start);
	//// Assert
	//ASSERT_EQ(startVal, 4);
	//ASSERT_THROW(r.attr(start) = 5, std::exception);

}