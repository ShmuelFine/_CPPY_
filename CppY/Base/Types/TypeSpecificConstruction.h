#pragma once

#include "object.h"
#include "FunDefs.h"

namespace py
{
	//object list(std::vector<object> const& v);
	//object dict(std::vector<std::vector<object>> const& v);
	//object str(object const& v);
}